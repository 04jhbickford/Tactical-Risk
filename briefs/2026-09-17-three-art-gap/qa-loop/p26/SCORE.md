# p26 SCORE — V2.81.51-three-polish.26
**Preview only** `?three=1`. Live Canvas untouched. Do not merge.
**Lineage:** PR50 `.25` kept (mid pip+N, cream `#F0E6D2`, idle quiet-dark, Confirm exclusive gold, recessed glyphs). Tesla off. Quiet James.

Stills pending first capture pass. Fail-closed: idle CTA is **not** gold; zoom is **not** mustard; mid has **zero** type parade; Confirm keeps exclusive gold.

## P0 HARD
| Gate | Verdict | Note |
|---|---|---|
| Idle CTA quiet-dark when nothing staged | **PASS (code)** | `#three-confirm.is-idle` stays `rgba(30, 36, 32, 0.88)` / `Select a territory`. Not `#C4A35A`. Stills next. |
| Gold / `--cta-confirm` only on named Confirm | **PASS (code)** | `.is-ready:not(:disabled):not(.is-idle)` is the only gold path. |
| Zoom chrome quiet frost, never mustard | **PASS (code)** | `#three-zoom button` is `rgba(30,36,32,0.62)` L0-family frost. No white→parchment gradient. No `#C4A35A`. |
| Mid = one cream pip + N ONLY | **PASS (code)** | Unchanged from .25. `paintPip` `glyph:null`. |
| L0 frost thin vibrancy not slab | **PASS (code)** | saturate 1.35 / blur 22 / fade `0.12 → 0`. |
| Mid tooth loud / near clean | **PASS (code)** | `TOOTH_NORMAL_MID 2.05` / `TOOTH_NORMAL_NEAR 1.08` via `applyLodTooth`. |

## BAR-POLYTOPIA-ROOT-TTR
Pending 390 still freeze (idle mid + near Confirm + HUD).

## Stills
- `europe-mid-390.png` — pending
- `europe-mid-hud-390.png` — pending
- `europe-near-select-390.png` — pending
- `europe-near-units-390.png` — pending
- `vercel-live-mid-390.png` — pending live `?three=1`

**PR:** stacked on PR50 / `.25`. Preview only.
