# SCORE — V2.81.53-ux-preview.5
**Preview only** `?three=1`. Do not merge. Tesla off. Quiet James.

```
artSource=main
uxSource=threePreview
sideProject=true
doNotMerge=true
iconPack=noOverlap
vizP1Coach=false
tryCombatMove=false
manualOnly=true
```

**Why:** James he-correct — no Try auto-start. Combat Move is FROM → units → TO → Confirm. Casualty pick when there is a choice.

## Layer B — start @390
| Gate | Verdict | Note |
|---|---|---|
| No Try button | **PASS** | Idle Confirm is disabled Select your stack. |
| Idle start cue | **PASS** | Pulse + FROM label + one-line tip ×. No Got it. |
| Manual Confirm gold | **PASS** | Gold only after origin + units + dest. |
| Unit chips | **PASS** | Gold fill + n/have steppers. Pulse + hint if dest and no ground. |
| Germans Ukraine→Karelia | **PASS** | Attack gray until unit tap; then Attack Karelia gold. |
| Casualty choice | **PASS** | 1 hit vs INF+FTR is not auto-assigned. Confirm after pick. |

## How-to (390, manual)
1. Tap pulsing origin stack.
2. Tap INF (and FTR if you want) — chips gold, steppers.
3. Tap enemy dest. Gold **Confirm: Move to …** / **Attack …**.
4. Battle: if you have a choice, tap the unit that takes the hit, then Confirm.
